# This workflow is DISABLED
# Using vercel.yml instead

name: Vercel Serverless Deploy (DISABLED)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
    - name: Workflow Disabled
      run: |
        echo "This workflow is disabled"
        echo "Using vercel.yml for deployment instead"
